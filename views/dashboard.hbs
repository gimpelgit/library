<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-body p-4">
      <h1 class="card-title text-primary mb-4">{{title}}</h1>
      <p class="lead">Это страница {{title}}. Функциональность будет добавлена позже.</p>
      <p class="mb-0">Вы вошли как: <strong>{{user.name}}</strong> ({{user.role_name}})</p>
    </div>
  </div>
</div>